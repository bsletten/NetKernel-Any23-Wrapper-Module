import net.bosatsu.gradle.*

apply plugin: 'netkernel'

nkmodule.modules = [
	"${project.projectDir}/module.xml"
]

dependencies {
	groovy localGroovy()
}

buildscript {
	repositories {
		mavenLocal()
	}
	
	dependencies {
        classpath group: 'net.bosatsu.gradle', name: 'nk-gradle-plugin', version: '0.0.5-SNAPSHOT'
	}
}

nkgreeting {
    greeting = 'You are rocking me'
}
